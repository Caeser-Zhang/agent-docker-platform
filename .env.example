# Copy to .env before `docker compose up -d`.
#
# Most configuration is in config/opencode.json (providers, MCP servers, etc.)
# and backend/.env (platform settings). This file is the optional top-level
# override for docker-compose variables.
#
# There are no required variables — the defaults work out of the box.